{
  "name": "glyphsmith",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "glyphsmith": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "dev": "pnpm run build && node dist/index.js host /private/tmp/glyphsmith-dev --host-port 6202 --strict-ports"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22",
    "typescript": "5.9.2"
  },
  "dependencies": {
    "@glyphsmith/ast": "workspace:*",
    "@glyphsmith/mcp": "workspace:*",
    "hono": "^4.12.23"
  }
}
